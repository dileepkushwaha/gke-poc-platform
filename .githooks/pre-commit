#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required. Install it, then retry the commit."
  exit 1
fi

gitleaks git --pre-commit --redact --staged --verbose
